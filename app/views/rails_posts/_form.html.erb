<%= form_with(model: rails_post, local: true) do |form| %>
  <% if rails_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rails_post.errors.count, "error") %> prohibited this rails_post from being saved:</h2>

      <ul>
      <% rails_post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
